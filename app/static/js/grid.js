

var horiz = document.getElementsByClassName("seven columns")[0].offsetWidth;

var vert =  document.getElementsByClassName("seven columns")[0].offsetHeight;


function create_grid(nrows, ncols) {
    var quotient = Math.floor(ncols/horiz);
    return p1 * p2;   // The function returns the product of p1 and p2
}

// TODO
